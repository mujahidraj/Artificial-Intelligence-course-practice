# Define the array
array = [10, 50, 100, 55, 95]

# Find the maximum value in the array
max_value = max(array)

# Print the maximum value
print("The maximum value in the array is:", max_value)
